name: Workflow of exercise 1
run-name: First workflow

on:
    push: 
        branches: [main]
    

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Hello world"
            

